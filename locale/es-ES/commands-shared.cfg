# Command responses shared across the team and admin command slices.
# Pre-seeded here so neither slice's cfg defines them (duplicate keys across
# files shadow silently; tools/check_locale.py fails them).

[mts-cmd]
player-only=Este comando solo puede usarlo un jugador.
team-not-exists=El equipo '__1__' no existe.
